# Session Handoff — Baltimore Health Dashboard (BALTI2)
Updated: 2026-07-08. Workflow: Codex works directly in `/Users/RomulloAtaides/Documents/Playground/BALTI2-work`, uses small diffs, verifies locally with Playwright/static checks, then pushes `codex/balti2-dashboard-work` to `rca2908/BALTI2` for GitHub Pages.

## Repo state
- Branch: `codex/balti2-dashboard-work`, tracking `fork/codex/balti2-dashboard-work`.
- Recent application-code commit: `a677d68 Fix ask data fallback and life expectancy display`; graph/handoff commits follow it on the same branch.
- GitHub Pages: deployed from `codex/balti2-dashboard-work` at `https://rca2908.github.io/BALTI2/`.
- Working tree tracked files are clean. Existing untracked local artifacts/backups remain (`.DS_Store`, `.Rhistory`, `.playwright-cli/`, `data/cache/`, `*.bak`, `index.html.pre311`, `data.json.pre-*`, Supabase temp files). Do not commit them unless explicitly requested.

## Completed & verified (this session cycle)
1. **BNIA extraction strategy**: all indicators are ArcGIS Feature Services under org mVFRs7NF4iFitgbY; catalog of 622 services at `data/metadata/bnia_service_catalog.csv`. `pv` wired to Hhpov in `update_data.R`.
2. **Data expansion (26 metrics)**: sm/ob/db/mh/pa (CDC PLACES), bb/rb/ui/gi (ACS tract, pop-weighted to CSA), fd→USDA 2019, ha (HUD CHAS Table 9), ap (EJScreen 2024 via Dataverse mirror — EPA removed EJScreen). Baltimore-only extracts cached in `data/cache/`. USALEEP validation at `data/metadata/usaleep_le_validation.csv`. data.json regenerated end-to-end (CENSUS_API_KEY in ~/.Renviron — REQUIRED for reruns or state/federal benchmarks degrade to scaffolds).
3. **Live 311 V1** (`live311.js` + 7 index.html edits): live layer from Open Baltimore ArcGIS (bbox query zoom≥15, 200 cap), SR popups with full status/Outcome, localStorage follows + batch refresh, ?sr= deep links, report wizard (emergency warning → pin+address → category → copyable packet → city portal handoff → paste-SR validation; manual candidate finder, never auto-attach). No public 311 submission API exists (Open311 endpoint is behind Salesforce login — city CDO credentials ask is the unlock). Test: `node /private/tmp/balti2-v311/test_live311.mjs`.
4. **Fixes**: Leaflet zero-size-container boot bug (ResizeObserver guard in index.html); dead Supabase project disabled in `supabase/config.js` (placeholder URL, flags false); Live311 report pin UX fixed so “Report 311” opens a clear map-pinning mode with a persistent guide, draggable report marker, marker/popup click capture, and a “Use this point” return to the packet form.
5. **HOLC overlay + metric popovers**: `index.html` now has a lazy-loaded Redlining Map layer from `holc_baltimore_1937.geojson` (59 graded polygons, HOLC pane z-index 425, legend + hover tooltips). The legend includes original paraphrased HOLC context copy, a CHDB "More about this feature" link, and grade filters for A/B/C/D with All grades as the default; all grades may also be unchecked to show no HOLC polygons. A single `ⓘ` metric info modal is backed by `METRIC_INFO`. Deliverables saved in `/private/tmp/balti2-holc/`; verified with `node /private/tmp/balti2-holc/check.mjs`.
6. **AI chat assistant code scaffold**: Cloudflare Worker lives in `worker/` with deploy-time bundled `dashboard-data.js`, Gemini free-tier provider call isolated in `askModel()`, structured JSON response schema, CORS allowlist, KV minute/day rate limits, daily request cap, and citation post-check. Frontend has an `Ask data` tool panel in `index.html`; it is inactive until deploy sets `window.BALTI2_CHAT_WORKER_URL` and Worker secret `GEMINI_API_KEY`.
7. **Next-pass dashboard polish**: `index.html` now has current-metric CSV export from the toolbar and city overview, concise source-quality badges (`Official`, `Proxy`, `Derived`, `Staged`) across metric UI, clearer Ask Data starter questions, and neighborhood map hover tooltips with metric value, city average, concern rank, and source quality.
8. **Local dashboard modules added while CHDB_API_KEY is held**: neighborhood click overview now includes a compact profile, top concerns, redlining centroid match, and closest current-profile comparison. City overview includes a redlining-by-current-metric panel from the HOLC layer. One- and two-neighborhood side panels now have a dedicated `311 hazards` tab for reported hazards, rat service requests, illegal dumping requests, and water/sewer requests; those sub-signals are no longer repeated in Determinants. The metric info modal now serves as the metric detail surface with reliability and coverage rows. Worker deterministic Ask Data now handles common rank, compare, change/trend, and basic interpretation questions before calling Gemini.
9. **Capital spending + 311 history local pass**: `index.html` reads the generated `supabase/seeds/spending_events_sample.sql` seed in-browser and turns it into a real Capital spending map mode, city overview module, one-neighborhood Capital tab, and two-neighborhood Capital comparison tab. The local seed parses to 1,247 records across 55 CSAs and about $2.386B when source amount fields are treated as thousands of dollars. The 311 hazards drilldown now adds since-2016 trend copy for each hazard sub-signal. Verified in the localhost preview: spending layer legend/source, one-pin capital tab, two-pin capital comparison, and 311 trend copy all render with no console errors.
10. **Report 311 pin UX fix**: `live311.js` keeps the same official-portal packet flow but makes pin mode explicit. The first modal now says this creates a Baltimore 311 packet, the map pin guide is larger, tells users to click the exact map spot then press Use this point, and `#map-wrap` gets a visible outline/crosshair while pinning. The 311 packet now has a visible address suggestion list from ArcGIS World Geocoding constrained to Baltimore; after the user pauses while typing a plausible address, the dashboard geocodes the top match, zooms to street level, hides the form, sets the report pin, and asks the user to confirm, drag, or click another spot before returning to the packet. Move/drop pin mode emits a pinning event that makes neighborhood polygons transparent and suppresses hover/click styling, so selected neighborhood colors and hover highlights cannot repaint the map while placing the 311 pin. Verified with `node /private/tmp/balti2-v311/test_report_pin.mjs`; Playwright confirmed type-to-map, return-to-form with address/coordinates, and zero-opacity neighborhood paths while pinning; preview copy refreshed with `rsync`.
11. **Ask Data + life expectancy repair**: `index.html` now answers common Ask Data questions locally before calling the Worker. Verified live on GitHub Pages: “Which neighborhood has the lowest broadband access?” returns Dorchester/Ashburton at 58.1% for 2023 with citation. Life expectancy no longer displays as `N/A` in the 2023 view; the UI uses the latest available BNIA neighborhood value (2018) and labels stale source years in tooltips, cards, CSV (`source_year`, `value_status`), metric detail, and Ask Data answers. Verified live: “What is life expectancy in Baltimore?” returns Baltimore CSA average 72.7 years for 2018, latest available. BNIA Lifexp service metadata itself lists neighborhood fields only through 2018.

## Current status / next work
- **Cloudflare Worker source changed but not redeployed**: `worker/index.js` now has GitHub Pages origins in the default CORS allowlist and shares the life-expectancy latest-available behavior. GitHub Pages is fixed without this because common Ask Data questions are answered locally, but run `npx wrangler deploy` from `worker/` when ready so the live Worker matches the repo.
- **CHDB API key still intentionally held**: `data/metadata/chdb_metric_crosswalk.tsv` covers CHDB metric IDs 14–60 and `scripts/fetch_chdb_metric_42.R` is ready, but CHDB_API_KEY work is on hold by user request.
- **Graphify refreshed**: `graphify update .` ran on 2026-07-08 after the latest patch and reported no code-graph topology changes. Generated graph outputs live in `graphify-out/`.

## Environment quirks
- Preview: launch config `balti2-dashboard` in `~/Downloads/.claude/launch.json` serves `/private/tmp/balti2-serve` (rsync copy — macOS TCC blocks the preview helper from ~/Documents). After ANY repo edit: `rsync -a --delete --exclude data/cache ~/Documents/Playground/BALTI2-work/ /private/tmp/balti2-serve/`. Browser heuristically caches index.html/config.js — force with fetch(url,{cache:'reload'}) then reload.
- Preview tab backgrounds → map container 0×0 → do all map interactions in ONE preview_eval (page reloads between evals).
- Codex MCP wrapper is broken (output schema error) — use the CLI via Bash. The codex-rescue agent is a forwarder that can't track its spawns — don't use it.
- Validation used this cycle: `node --check worker/index.js`; inline `<script>` parse via Node `vm.Script`; Playwright local and live checks for Ask Data and life expectancy; `curl -I` for GitHub Pages assets.

## Suggested skills
- `graphify`: use before broad codebase questions or after code changes that should refresh the graph.
- `impeccable` + `web-design-guidelines`: use for dashboard UI/UX polish.
- `karpathy-guidelines`: use for surgical debugging and avoiding overbuilt changes.
- `playwright`: use for browser verification of map/chat/report flows.

## Ideas backlog (from NYU CHDB gap analysis, ranked)
CI/reliability layer (PLACES lo/hi + ACS MOE passthrough, small-pop flags) → peer-city comparison via CHDB API (user must request free key at cityhealthdashboard.com/data-access) → metric-vs-metric scatterplot (Chart.js already loaded). Skipped deliberately: custom areas, race-stratified breakdowns (sources don't support at tract), rebuilding CHDB's multi-city machinery.

## People / asks pending
- City CDO: Salesforce/Open311 API credentials (unlocks direct 311 submission with photos) — "Phase 0" per Codex review at /private/tmp/balti2-ext/review_output.md.
- BNIA direct contact: real asthma ED rate series (public org has none; `as` stays a CDC proxy).
- NYU CHDB API key request (for peer-city benchmarks).
